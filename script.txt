proc recurse x {
    if {le <x> 0} {
        return a
    } else {
        set t [recurse [sub <x> 1]]
        return <t>[chr [add <x> [ord a]]]<t>
    }
}
puts (this is recursion test)
puts [recurse 7]
